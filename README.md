# live-service (APIs de fútbol)
## Endpoints
- `GET /health`
- `GET /matches?competitions=WC`  (football-data.org)
- `GET /fixtures?live=all`        (API-FOOTBALL)

## Local
```bash
npm i
npm start
# http://localhost:4000/health
```

## Env
- FOOTBALL_DATA_TOKEN
- API_FOOTBALL_KEY
- PORT (opcional)
